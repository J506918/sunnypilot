name: Generate Fonts

on:
  push:
    branches:
      - staging-zh

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@v2

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1-mesa-dev libxi-dev libxrandr-dev libxinerama-dev

      - name: Install dependencies
        run: |
          pip install cffi pyray

      - name: Build Fonts
        run: |
          # Command to build fonts
          echo "Building fonts..."

