import os\nimport time\nimport logging\nimport subprocess\nfrom pathlib import Path\n\n# Initialize logging\nlogging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s',\n                    handlers=[logging.FileHandler("translation_monitor.log"), logging.StreamHandler()])\n\n# Define constants\nDIRECTORY_TO_WATCH = Path("translations")\nRETRY_LIMIT = 3\nRETRY_DELAY = 5\n\ndef run_font_generation(language_code):\n    \